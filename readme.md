1. opening a new virtual environemnt (make sure to navigate into this project's main directory first):
    python -m venv exercise1_venv

2. activating the virtual environment:
    a. 
        cd exercise1_venv/Scripts
    b.
        activate
    c.
        cd ../..

3. installing all relevant libraries:
    python -m pip install -r requirements.txt

4. running the main file:
    python insurance.py